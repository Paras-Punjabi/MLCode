kind: Service
apiVersion: v1
metadata:
  name: minio
  namespace: mlcode
spec:
  type: ClusterIP
  selector:
    app: minio-pod
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9090
      targetPort: 9090
