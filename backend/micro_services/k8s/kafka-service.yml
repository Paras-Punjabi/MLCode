apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: mlcode
spec:
  type: ClusterIP
  selector:
    app: kafka-pod
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093